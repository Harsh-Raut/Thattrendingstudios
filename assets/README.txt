Drop media here.

playback-moment.mp4   → the hero "playback moment" clip (10–20s, muted-friendly,
                        a real singer hearing themselves for the first time).
                        Landscape works best. Keep it small (<8 MB) for fast load.
playback-poster.jpg   → optional still shown before the video loads.

bg-loop.mp4           → OPTIONAL shared background loop for the How / Proof / Why /
                        Testimonials sections (abstract, subtle: light bokeh, drifting
                        particles, or a soft waveform). Muted, seamless loop, small.
                        Rendered very faint (opacity ~14%, multiply blend) behind
                        content. Until it's added, those sections just show the
                        drifting brand-colour orbs — no broken/empty state.

Until you add playback-moment.mp4, the section shows a branded blue/green gradient
with the caption overlay — it still looks intentional, just without motion.
